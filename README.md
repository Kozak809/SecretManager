# Secret Manager
Легковесное и защищенное десктопное приложение для локального хранения конфиденциальных заметок и паролей. Написано на Python с использованием криптографии промышленного уровня.
## 🔒 Безопасность
* **Алгоритм шифрования:** AES-256-GCM (Authenticated Encryption).
* **Генерация ключей:** PBKDF2HMAC (SHA-256) с 390,000 итераций и уникальной солью для каждого файла.
* **Защита памяти:** Реализован механизм `secure_zero_memory` для очистки ключей и паролей из RAM сразу после использования.
* **Удаление:** Безопасное удаление файлов (перезапись случайными байтами перед удалением).
* **Anti-Debug:** Встроенная проверка наличия отладчика.
## 🚀 Установка и запуск
### Запуск из исходного кода
1. Установите Python 3.x.
2. Установите зависимости:
```bash
pip install -r requirements.txt
```
3. Запустите приложение:
```bash
python main.py
```
### Запуск EXE
Просто запустите файл `SecretManager.exe`. Папка `secrets`, в которой хранятся зашифрованные файлы, будет создана автоматически рядом с исполняемым файлом.
## ⚠️ Дисклеймер
Приложение предоставляется "как есть". Потеря пароля от файла приведет к полной невозможности восстановления данных, так как алгоритм шифрования не имеет бэкдоров.
